import React from 'react';
import ReactDOM from 'react-dom/client'; // React 18 ile gelen yeni yöntem
import App from './app'; // Düzgün dosya adıyla

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
